import { shallowMount } from "@vue/test-utils"

describe('Alert', () => {
    describe('Close button', ()=> {
        it('close when the user clicks the close button', () => {
            const wrapper = shallowMount(Alert);
        })
    })
})